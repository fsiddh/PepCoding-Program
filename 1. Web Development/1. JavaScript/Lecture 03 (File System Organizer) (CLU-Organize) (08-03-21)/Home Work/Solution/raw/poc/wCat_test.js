let fs = require("fs");
let path = require("path");

let singleFileFn = require

let input = process.argv.slice(2);
// console.log(input);

let cmd = input[0];
// console.log(cmd);

switch(cmd){
    case "-s":

        break;
    case "-n":

        break;
    case "-b":

        break;
    default:
        if (cmd.length == 1){
            let 
        }
        else{

        }
}

